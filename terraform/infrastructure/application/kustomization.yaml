# -----------------------------------------------------------------------------
#
# Application Kustomization
#
# Purpose:
# Defines the Kubernetes resources that make up the application workloads.
#
# -----------------------------------------------------------------------------

resources:
  # Existing monolith - retained temporarily during the nine-service migration.
  - application-deployment.yaml
  - application-service.yaml
  - application-serviceaccount.yaml

  # New nine-service architecture.
  - api-gateway
