apiVersion: v1
kind: Service

metadata:
  name: application
  namespace: jenkins

spec:
  selector:
    app: application

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

  type: ClusterIP