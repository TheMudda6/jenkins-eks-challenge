apiVersion: v1
kind: Service
metadata:
  name: worker
  labels:
    app: worker
  namespace: jenkins
spec:
  type: ClusterIP
  selector:
    app: worker
  ports:
    - name: http
      port: 8090
      targetPort: http
