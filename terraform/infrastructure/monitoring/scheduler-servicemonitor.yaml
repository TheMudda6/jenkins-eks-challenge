apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: scheduler
  namespace: monitoring
  labels:
    monitoring: prometheus
spec:
  namespaceSelector:
    matchNames:
      - jenkins
  selector:
    matchLabels:
      app: scheduler
  targetLabels:
    - app
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
