apiVersion: v1
kind: Service
metadata:
  name: dashboard-api
  labels:
    app: dashboard-api
  namespace: jenkins
spec:
  type: ClusterIP
  selector:
    app: dashboard-api
  ports:
    - name: http
      port: 8086
      targetPort: http
