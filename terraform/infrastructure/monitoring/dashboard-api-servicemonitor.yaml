apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dashboard-api
  namespace: monitoring
  labels:
    monitoring: prometheus
spec:
  namespaceSelector:
    matchNames:
      - jenkins
  selector:
    matchLabels:
      app: dashboard-api
  targetLabels:
    - app
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
