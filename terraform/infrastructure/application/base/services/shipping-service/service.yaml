apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  labels:
    app: shipping-service
  namespace: jenkins
spec:
  type: ClusterIP
  selector:
    app: shipping-service
  ports:
    - name: http
      port: 8085
      targetPort: http
